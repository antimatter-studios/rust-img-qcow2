version: "3"
chore_min_version: 0.6.0

# THE CONTRACT THIS FILE EXISTS TO OWN.
#
# How to build rust-img-qcow2 is knowledge that belongs to rust-img-qcow2. It used to live in
# diskjockey/scripts/build-img-containers.sh, a 205-line script that knew the
# target triple, the cargo profile, the `[lib].name` of four crates and where
# cargo puts things — none of which is a consumer's business.
#
# `staticlib` is the whole interface a consumer needs, AND IT TAKES NO
# OUTPUT DIRECTORY. It builds into this crate's own dist/; `chore artifact`
# prints the absolute path of that DIRECTORY, and a consumer copies its
# contents. It is the same pair of tasks, with the same contract, that
# every sibling library exposes.
#
# `artifact` PRINTS A DIRECTORY, NOT A FILE. That is the one place this
# differs from rust-blk-probe, whose single binary lets its `artifact` name
# a file: this crate produces libqcow2.a AND the headers beside it, so
# there is no single path to name.
#
# WHY fs_core.h IS COPIED TOO. include/qcow2.h opens with
# `#include "fs_core.h"` — this crate's C ABI hands out FsCoreDevice handles —
# so a consumer given only qcow2.h cannot compile. The path below is the same
# one Cargo.toml resolves am-fs-core from, and NOTHING CHECKS THAT THE TWO
# AGREE.
# QUIET BY DEFAULT, --verbose ON REQUEST, AND THE BUDGET IS A NUMBER.
#
#   A test task prints a VERDICT: one line per tier, a final count, and the
#   path of the log holding everything else. A failure prints the TAIL of
#   that log. `chore test -- --verbose` (or FLTH_VERBOSE=1) streams the whole
#   run as it happens.
#
#   Nothing is thrown away: every tier's output goes to tmp/logs/<tier>.log
#   in full. What changed is where it is read from.
#
#   Each tier carries an OUTPUT BUDGET -- a line count and a byte count that
#   fail the run (exit 65) when the tier prints more than it is allowed to,
#   told apart from a failing suite by its status. The budgets below are
#   MEASURED -- the numbers below are the verdict lines of CI run
#   35437091960, which is the largest of the three matrix legs in each case;
#   an arm64 Linux host measures within one line of them:
#
#     tier     measured        budget         headroom, and why
#     debug      304 / 19,554    420 / 28,000  ~38% / ~43%: 248 executed
#                                              tests at one line each, so
#                                              this is "about a hundred more
#                                              tests before anyone has to
#                                              think about it". The job has a
#                                              separate build step, so no
#                                              `Compiling` lines land here
#     release    307 / 19,760    420 / 28,000  the same selection in the
#                                              other profile, and the same
#                                              headroom. The job has no
#                                              separate build step, so a
#                                              cold cache puts ~75
#                                              `Compiling` lines in here too
#     qemu        36 /  2,133    130 /  9,000  25 tests at 36 lines, plus the
#                                              ~75 `Compiling` lines a cold
#                                              cache puts in this tier: the
#                                              job has no build step, and the
#                                              feature gate means this target
#                                              is built nowhere else
#     lib32       53 /  3,779    200 / 14,000  the 42 library tests, plus the
#                                              i686 build's own `Compiling`
#                                              lines, which land in this tier
#                                              because the job has no build
#                                              step of its own
#
#   Raise one DELIBERATELY when a tier grows, the way the executed-test
#   floors are raised -- and raise it in .github/workflows/ci.yml at the same
#   time, because tests/ci_profile.rs fails when the two files disagree. A
#   budget nobody can breach measures nothing.
#
#   THE FLOORS ARE THE OTHER HALF. A budget fails a tier that PRINTS too
#   much; nothing fails a tier that prints almost nothing because it RAN
#   almost nothing, and `cargo test` exits 0 on "0 passed; 0 failed" (#99).
#   So every tier also says how few executed tests still count as having run
#   it. Measured, and they only ever go up.
#
#   Why it is worth a task's while: a passing run that prints three thousand
#   lines hides the twenty that matter, and every reader pays for it -- a
#   person scrolling, a CI log viewer, and an agent working here, which
#   re-reads its transcript on every step and so pays for one loud run many
#   times over. Measured across this constellation: 4,661M cache-read tokens
#   against 9.5M of output, with command output the largest single
#   contributor a repository controls.
#
#   WHERE THE SCRIPT CAME FROM. scripts/output-budget.sh is a vendored copy
#   of fs-linux-test-harness's, at v0.1.0; the header of that file says why a
#   copy rather than the sibling the filesystem crates read it from, and
#   tests/output_budget.rs holds the copy to the behaviour rather than to the
#   bytes.
vars:
  TRIPLE: aarch64-apple-darwin
  # `[package].name` is am-img-qcow2; `[lib].name` is qcow2, so cargo writes
  # libqcow2.a. The header mirrors the lib name, not the package name.
  LIBNAME: qcow2

tasks:
  staticlib:
    desc: Build the static library and its headers into this crate's dist/
    # NO `out` ARGUMENT, deliberately. A library does not write into its
    # consumer. It builds into its own dist/ and a consumer asks where that
    # is — `chore artifact` prints the path. Inverting it that way means this
    # crate can change its output layout without every consumer knowing, and
    # it stays independently usable with no consumer at all.
    vars:
      OUT: 'dist'

    # Freshness, replacing the stamp-file and `find -newer` mtime comparison
    # the old script carried. Content checksums, so touching a file does not
    # trigger a rebuild and reverting one does not hide behind a newer mtime.
    #
    # These patterns are LITERAL on purpose. chore records a fingerprint by
    # calling fingerprint.Save WITHOUT a renderer (internal/run/run.go:313),
    # so a pattern containing {{ }} is expanded against the raw text, matches
    # nothing, and stores the empty-set hash — the task then never registers
    # as up to date.
    sources:
      - Cargo.toml
      - 'src/**/*.rs'
      - 'include/*.h'
      # fs_core.h is a source of ours because we ship it (see the cp below).
      # THIS PATH AND THE cp MUST AGREE, and nothing checks that they do.
      - '../rust-fs-core/include/fs_core.h'
      # THE GUARD'S OWN IMPLEMENTATION AND ITS INPUT ARE SOURCES OF
      # THIS TASK. The first command below is a test, so the file that
      # test lives in decides what the task checks, and chores.yml is
      # what it reads LIBNAME out of. Left out of this list, editing
      # either does not change the fingerprint: measured on this crate,
      # with the guard's body replaced by `panic!`, a second `chore
      # staticlib` printed "task: staticlib is up to date", exited 0,
      # and never ran it -- while `cargo test --locked --test
      # header_names_the_built_library` on the same tree exited 101. A
      # check that a stale fingerprint can skip is not a check.
      #
      # A GLOB RATHER THAN THE ONE FILENAME, because a literal path
      # that stops matching after a rename fails silently -- the same
      # shape as the guard it is protecting. Every other test here is
      # cheap to over-declare: the cost is re-running a task that is
      # already correct.
      - 'tests/**/*.rs'
      - chores.yml

    # Templated, which is fine here where it is not fine above: UpToDate
    # renders these before expanding them, and every entry is an exact path
    # rather than a glob, so a missing artifact is caught by the pattern
    # failing to match.
    generates:
      - '{{.OUT}}/lib{{.LIBNAME}}.a'
      - '{{.OUT}}/include/{{.LIBNAME}}.h'

    cmds:
      # THE HEADER CHECK IS THE TEST, NOT A SECOND COPY OF IT.
      #
      # This was a shell fragment that grepped the header and compared
      # what it found against `lib{{.LIBNAME}}.a` -- a value derived
      # from the same hand-maintained LIBNAME variable it was checking.
      # A LIBNAME/Cargo.toml drift, which is precisely what it existed
      # to catch, passed it silently: rename `[lib] name` and cargo
      # builds a different artefact while the guard compares LIBNAME
      # against itself and agrees. A check whose two sides come from one
      # source cannot fail.
      #
      # tests/header_names_the_built_library.rs derives the expected
      # name from Cargo.toml's `[lib] name` -- the thing that actually
      # decides the artefact -- with a real TOML parser rather than a
      # line scan. Running it here means one implementation instead of
      # two, and the one that is already exercised by CI.
      #
      # It stays FIRST so a mismatch costs no release build.
      - 'cargo test --locked --test header_names_the_built_library'
      # Idempotent, and it replaces the old script's "is the target installed"
      # `rustup target list --installed | grep` dance: rustup answers "up to
      # date" without touching the network when it is. It runs here rather
      # than in the consumer because the triple is ours.
      - 'rustup target add {{.TRIPLE}}'
      - 'cargo build --release --target {{.TRIPLE}}'
      - 'mkdir -p "{{.OUT}}/include"'
      - 'cp target/{{.TRIPLE}}/release/lib{{.LIBNAME}}.a "{{.OUT}}/lib{{.LIBNAME}}.a"'
      - 'cp include/{{.LIBNAME}}.h "{{.OUT}}/include/{{.LIBNAME}}.h"'
      - 'cp "../rust-fs-core/include/fs_core.h" "{{.OUT}}/include/fs_core.h"'

  build:
    desc: Debug build
    cmds: ['cargo build']

  test:
    desc: 'Every tier the gate runs: debug (the only profile that can see an overflow), release (the profile that ships), and the qemu-img cross-validation'
    # NOT `cargo test`. That ran the debug tier and nothing else, so the
    # two runs CI gates on -- the release profile and the qemu-img
    # cross-validation -- were first exercised after the push. These are
    # the same four tasks .github/workflows/ci.yml runs, with the same
    # budgets and the same floors; tests/ci_profile.rs checks that the
    # two files still agree.
    #
    # test:lib32 is NOT here: it needs a 32-bit C toolchain
    # (gcc-multilib) and the i686 rustup target, which is a runner's
    # business rather than every developer's. It is a task so it can be
    # run by hand, and CI runs it.
    cmds:
      - task: test:debug
      - task: test:release
      - task: test:qemu
      # THE LAST LINE: one number across the tiers, and where the detail is.
      - bash scripts/test-verdict.sh debug release qemu

  test:debug:
    desc: 'The suite in the debug profile, where overflow checks are on'
    # EXPECT_OVERFLOW_CHECKS ARMS THE ONE CHECK A TEXT SCAN CANNOT DO:
    # `overflow_checks::the_build_the_gate_asked_to_check_does_check` in
    # src/lib.rs asks the build to prove it traps, rather than inferring
    # it from which spellings of "disabled" are absent from Cargo.toml.
    # Do not add `--release` here; tests/ci_profile.rs refuses a
    # workflow with no such run, and the reason is the same one.
    cmds:
      - 'EXPECT_OVERFLOW_CHECKS=1 bash scripts/tier.sh "test (debug)" debug 420 28000 -- cargo test --locked --all-targets'
      - 'bash scripts/test-floor.sh debug 225'

  test:release:
    desc: 'The suite in the profile that ships: no debug_assert!, arithmetic wraps, lto = true'
    # NOT A DUPLICATE OF test:debug. `debug_assert!` bodies vanish under
    # `--release` and arithmetic wraps instead of panicking, so a test
    # guarding a wrapping computation passes the debug run for the WRONG
    # reason. See the long note in .github/workflows/ci.yml on the
    # `test-release` job, and #98.
    cmds:
      - 'bash scripts/tier.sh "test (release)" release 420 28000 -- cargo test --locked --release --all-targets'
      - 'bash scripts/test-floor.sh release 225'

  test:qemu:
    desc: 'The qemu-img cross-validation: another implementation reads what we wrote'
    # NEEDS qemu-img ON PATH, and does not skip without it: the helpers
    # in tests/qemu_validation.rs go through `run_qemu`, which panics
    # naming the package when qemu-img cannot be spawned. That was #97 --
    # a probe that returned early on every runner and counted as passed.
    #
    # Both halves of the selection matter. The `[[test]]` entry in
    # Cargo.toml carries `required-features = ["qemu-validation"]`, so
    # without the feature cargo does not build the target at all, and
    # `cargo test --test qemu_validation` alone matches nothing and exits
    # 0 (#50).
    cmds:
      - 'bash scripts/tier.sh qemu-validation qemu 130 9000 -- cargo test --locked --features qemu-validation --test qemu_validation'
      - 'bash scripts/test-floor.sh qemu 23'

  test:lib32:
    desc: 'The library unit tests on a 32-bit target, where `as usize` narrows'
    # A 32-bit target, because some defects only exist there: a `u64`
    # narrowed with `as usize` keeps its low 32 bits, and on a 64-bit
    # host every `u64` fits, so the test for it is
    # `#[cfg(target_pointer_width = "32")]` and compiles out of every
    # other tier (#62).
    #
    # NEEDS `rustup target add i686-unknown-linux-gnu` and a 32-bit C
    # toolchain (gcc-multilib on Debian). It fails naming them rather
    # than skipping.
    cmds:
      - 'bash scripts/tier.sh "test (32-bit lib)" lib32 200 14000 -- cargo test --locked --lib --target i686-unknown-linux-gnu'
      - 'bash scripts/test-floor.sh lib32 38'

  clean:
    desc: Remove this crate's cargo output
    cmds: ['cargo clean']

  artifact:
    desc: Print the absolute path of the directory holding the built outputs
    # `silent: true` is what makes this usable as a VALUE — without it chore
    # echoes each command and a caller capturing stdout gets the echo too.
    # PATTERNS.md: "a value — a flag string, a container status, a path" is a
    # task called through {{.CHORE_EXE}}.
    silent: true
    # NO `deps: [staticlib]`. chore prints "task: staticlib is up to date" on
    # STDOUT, which a caller capturing this as a value receives as PART OF THE
    # VALUE — measured on rust-blk-probe, where the capture came back as two
    # lines and the caller's path check failed. A value-returning task returns
    # a value and does nothing else; the caller builds first, then asks. Two
    # calls, on purpose.
    #
    # IT PRINTS A DIRECTORY, NOT A FILE, and that is the one place this differs
    # from rust-blk-probe's `artifact`. That crate produces a single binary, so
    # it can name a file. This one produces libqcow2.a AND the headers beside
    # it, so there is no single path to name — A CONSUMER COPIES THE CONTENTS of
    # what this prints. A consumer that treats it as a file path will silently
    # copy the wrong thing.
    cmds:
      - 'cd "{{.OUT}}" && pwd -P'
    vars:
      OUT: 'dist'

  lint:
    desc: Formatting and clippy, exactly as CI runs them
    cmds:
      - scripts/agents-core-check.sh
      - cargo fmt --check
      - cargo clippy --locked --all-targets -- -D warnings
